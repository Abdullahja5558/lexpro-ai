/** @type {import('next').NextConfig} */
const nextConfig = {
  // Isay experimental se nikaal kar bahar rakhein
  allowedDevOrigins: ['192.168.10.51', 'localhost:3000'],
  
  /* Agar koi aur settings hain toh yahan niche aayengi */
};

export default nextConfig;